/*
    assignment 1.0 | creating a controller
    create a controller named "SearchCtrl" and add it to the "MovieFInder" module we made in assigment-1
    syntax:
        angular
            .module('YourModule')
            .controller('YourControllerName', YourControllerFunction);
        function YourControllerFunction () {}
*/

/*
    assignment 1.2 | binding data
    - inject angulars '$scope' service into our function by adding it as a parameter to our SearchCtrl function
    - set a property of $scope call it "searchQuery" e.g "$scope.searchQuery" and set the value of it to a string with some random text
*/
